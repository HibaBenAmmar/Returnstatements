//checking if a number is bigger than 100

const checking = function (number) {
    const sum = number;
    if (sum > 100) {
        return "true";
               
    }
    return "false";
    
};

const result = checking (101);
console.log (result);

//Bouncer at a club...check number of people in club...check age.




//calculating the average

const calculateAverage = function (numbers) {
const average = numbers.average
};

const grades = [6,9,5,2,14];
calculateAverage (grades);























